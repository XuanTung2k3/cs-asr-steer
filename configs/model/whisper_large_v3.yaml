model:
  id: /mnt/data/tungnx/whisper-large-v3
  hub_id: openai/whisper-large-v3
  revision: null              # resolved and recorded at first run (local snapshot hash)
  dtype: bfloat16
  device: cuda
  freeze_backbone: true
  output_hidden_states: true
  use_cache: true
  attn_implementation: eager  # required: cross-attentions are needed for E1 DTW alignment
  local_files_only: true
